/**
 * ⚠️ このファイルは凍結中です ⚠️
 *
 * ファイル管理機能は使用率が低いため凍結されています。
 * 将来的にSupabase Storageへの移行を予定しています。
 */

import { StyleSheet } from "react-native";

// ダミーエクスポート（importエラー防止）
export const styles = StyleSheet.create({});
